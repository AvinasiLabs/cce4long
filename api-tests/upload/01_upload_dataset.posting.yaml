name: Upload dataset
description: Upload raw data for a dataset. cced encrypts it and stores to the configured backend.
method: POST
url: http://localhost:3000/v1/datasets/:dataset_id/upload
headers:
  - name: Content-Type
    value: application/octet-stream
body:
  content: "hello world from posting"
path_params:
  - name: dataset_id
    value: '1'
